# ── Dev profile: local Postgres via Docker or local install ───────────────────
spring.datasource.url=jdbc:postgresql://localhost:5432/eduflow
spring.datasource.username=eduflow_user
spring.datasource.password=change_me_strong_password

spring.jpa.show-sql=true

# Swagger UI enabled in dev
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true

app.jwt.secret=dev-only-secret-at-least-32-chars-long!!
